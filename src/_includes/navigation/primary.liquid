<nav aria-label="Primary">
  <ul>
    {% for nav in collections.primary %}
    <li>
      <a {% if page.url==nav.url %}aria-current="page" {% endif %} href="{{ nav.url | url }}">{{ nav.data.title }}</a>
    </li>
    {% endfor %}
  </ul>
</nav>